services:
  agentflow:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./output:/app/output
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
